/**
 * LayoutController — CSS Flexbox pane stack (Core 3.0 Multi-Chart).
 * Geometry lives in HTML/CSS; ChartAdapter resizes LWC instances.
 */
const LayoutController = {
  init() {},
  loadPaneHeights() {},
};

window.LayoutController = LayoutController;
