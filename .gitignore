# Secrets / local config
.env
config/matrix.json

# SQLite archives (any location, incl. test artifacts)
*.db
*.db-wal
*.db-shm
history.db*
*.sql
rsx_settings.json
rsx_settings.json.tmp

# Backups / data caches
backups/
data/exchange_limits_cache.json
server/data/
server/config/
server/server/

# Compiled binaries / test artifacts
trading_bot
# Research artifacts (local FeatureTape / LabelSet dumps; not identity SSOT)
research/tapes/
research/labels/
*.test
*.exe
*.out

# Agent / IDE local state (rules and mcp example stay tracked)
.cursor/*
!.cursor/rules/
!.cursor/mcp.json.example
.cursor/mcp.json

# OS junk
.DS_Store
